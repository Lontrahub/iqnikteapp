steps:
  # Install dependencies
  - name: 'gcr.io/cloud-builders/npm'
    args: ['install']
  # Build the application
  - name: 'gcr.io/cloud-builders/npm'
    args: ['run', 'build']
  # Deploy to Firebase
  - name: 'gcr.io/google-appengine/firebase'
    args: ['deploy', '--only', 'hosting']
